type copy A {}

type pub copy B {}

type copy enum C {}

type pub copy enum D {}

type copy async E {}

type copy extern F {}

# copy_type_definitions.inko:9:17 error(invalid-type): only regular and 'enum' types support the 'inline' and 'copy' keywords
# copy_type_definitions.inko:11:18 error(invalid-type): only regular and 'enum' types support the 'inline' and 'copy' keywords
