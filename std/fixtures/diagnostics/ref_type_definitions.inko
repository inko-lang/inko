type ref A {}

type pub ref B {}

type ref enum C {}

type pub ref enum D {}

type ref async E {}

type ref extern F {}

# ref_type_definitions.inko:9:16 error(invalid-type): only regular and enum types support the 'inline', 'copy' and 'ref' keywords
# ref_type_definitions.inko:11:17 error(invalid-type): only regular and enum types support the 'inline', 'copy' and 'ref' keywords
