import std.stdio (Stdout)

let pub STRING = 'foo'
let pub INT = 42
let pub BOOL = true
let PRIVATE = 42

class async Main {
  fn async main {
    let out = Stdout.new

    out.print(STRING)
    out.print(INT.to_string)
    out.print(BOOL.to_string)
  }
}
