# Types for cryptographic ciphers.

# A cryptographic cipher.
trait pub Cipher {
  # Encrypts the data in the given `ByteArray`, modifying it in-place.
  fn pub mut encrypt(bytes: mut ByteArray)

  # Encrypts the data in the given `ByteArray`, modifying it in-place.
  fn pub mut decrypt(bytes: mut ByteArray)
}
