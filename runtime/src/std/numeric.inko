# Common numerical operations.

# A trait providing common numerical operations.
trait Numeric {
  # Returns `True` if the number is a negative number.
  def negative? -> Boolean

  # Returns `True` if the number is a positive number.
  def positive? -> Boolean

  # Returns `True` if the number is equal to zero.
  def zero? -> Boolean

  # Returns the absolute value of `self`.
  def absolute -> Self

  # Returns a copy of `self` with the opposite sign.
  def opposite -> Self
}
