docs:master:
  extends:
    - .only-docs-changes
  image: registry.gitlab.com/inko-lang/development-docker-images:alpine
  stage: docs
  script:
    - make docs/publish
  only:
    refs:
      - master@inko-lang/inko
