import std::test::Tests
import helpers::(fmt)

fn pub tests(t: mut Tests) {
  t.test('Nil.==') fn (t) {
    t.equal(nil, nil)
  }

  t.test('Nil.clone') fn (t) {
    t.equal(nil.clone, nil)
  }

  t.test('Nil.fmt') fn (t) {
    t.equal(fmt(nil), 'nil')
  }
}
